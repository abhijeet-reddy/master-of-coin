# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_secure_password
POSTGRES_DB=master_of_coin

# Backend Configuration
# Note: DATABASE_URL is constructed automatically in docker-compose.yml
# For local development: postgresql://postgres:postgres@localhost:5432/master_of_coin
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/master_of_coin

# JWT Configuration (REQUIRED - minimum 32 characters)
JWT_SECRET=change_this_to_a_secure_secret_min_32_chars_for_production_use

# JWT token expiration in hours (default: 24)
JWT_EXPIRATION_HOURS=24

# Server Configuration (optional - defaults provided)
SERVER_HOST=0.0.0.0
SERVER_PORT=13153

# Logging Configuration
RUST_LOG=info

# Database Connection Pool (optional - default: 10)
DATABASE_MAX_CONNECTIONS=10

# Frontend Configuration (for local development only)
# In production, frontend is served by the backend
VITE_API_URL=http://localhost:3000

# Exchange Rate API Configuration
# Get your free API key from https://www.exchangerate-api.com/
EXCHANGE_RATE_API_KEY=your_api_key_here

# Data Directory Configuration (optional)
# DATA_DIR=/var/lib/master-of-coin  # Optional: defaults to ./data if not set
#
# This variable controls where Docker volumes store persistent data (PostgreSQL and Redis).
#
# Default behavior (if not set or commented out):
#   - Uses ./data relative to the project root
#   - Creates ./data/postgres and ./data/redis directories
#
# Custom path examples:
#   - Relative path: DATA_DIR=./custom-data (relative to project root)
#   - Absolute path: DATA_DIR=/var/lib/master-of-coin (for external storage)
#   - External drive: DATA_DIR=/mnt/storage/master-of-coin-data
#
# Notes:
#   - The directory must exist and have proper permissions before starting Docker
#   - For production, consider using an absolute path on a dedicated volume
#   - Ensure the directory is included in your backup strategy